# Build artifacts de Nix
result
result-*

# Environnements virtuels
.direnv/
.devenv/

# Fichiers de secrets bruts (l'IA ne doit pas lire les clés privées)
# Note: On garde l'accès à .sops.yaml pour comprendre l'architecture,
# mais on évite les dumps de clés s'il y en a.
keys/
